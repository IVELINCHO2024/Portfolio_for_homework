// Online C compiler to run C program online
#include <stdio.h>

int strlength(char *str){
    int size = sizeof(str)/sizeof(str[9]);
    printf("%d",size);
}

int main(){
    
    strlength("iv");
   
    return 0;
}